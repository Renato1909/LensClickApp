# Matriz de paridade — web, API e Android

**Auditoria:** 19 de setembro de 2026, fuso America/Sao_Paulo (20 de setembro em UTC).

**Estado:** inventário do código e proposta de escopo; não é aprovação de lançamento nem contrato móvel. Todas as classificações abaixo dependem do aceite do mantenedor. Riscos de adiamento estão documentados, mas ainda não foram aceitos.

## Fontes e limites da evidência

- Plataforma: `Renato1909/LensClick`, `main` confirmada pela API do GitHub e cópia local no commit [`c3358c2bb7e263095e9dea590f2b7f00add43ef0`](https://github.com/Renato1909/LensClick/commit/c3358c2bb7e263095e9dea590f2b7f00add43ef0), PR #127. Foram consultados roteador, handlers, esquema/migrações, UI web e catálogo/testes de contratos.
- Android: base [`bd3c1408886b0279a1f8867805bd93785939bf21`](https://github.com/Renato1909/LensClickApp/commit/bd3c1408886b0279a1f8867805bd93785939bf21), com 17 telas, repositório local, DAO e entidades Room. A matriz descreve a capacidade funcional dessa base; a entrega de fundação não adiciona integração remota.
- A consulta foi somente leitura. Não foram acessados D1/R2 remotos, contas de usuários ou endpoints da implantação; não houve deploy, migração, envio de e-mail ou escrita na plataforma. Código em `main` não comprova sozinho o comportamento da produção.
- Os testes da plataforma citados foram inspecionados, não executados nesta auditoria. A validação Android da entrega está registrada em `HANDOFF.md`; ainda não há teste de contrato Android/API.
- O roteador e `test-support/api-contracts.mjs` têm **45 operações HTTP**. Documentos anteriores da plataforma mencionam 40 rotas; o catálogo executável é a referência desta matriz.

Fontes fixadas no commit auditado:

| Referência | Evidência |
|---|---|
| W1 | [Roteador HTTP](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/router.mjs) e [catálogo de contratos](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/test-support/api-contracts.mjs) |
| W2 | [Autenticação](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/auth.mjs) e [sessão, DTOs e origem no Worker](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/worker.mjs) |
| W3 | [Segurança e edição da conta](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/account-security.mjs) e [exportação, inbox e mensagens](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/account.mjs) |
| W4 | [Descoberta, perfil profissional e avaliações](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/photographers.mjs) |
| W5 | [Portfólio](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/portfolio.mjs) e [imagens](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/images.mjs) |
| W6 | [Administração](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/admin.mjs), [entradas públicas](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/src/public-input.mjs) e [esquema D1](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/db/schema.ts) |
| W7 | [UI web](https://github.com/Renato1909/LensClick/tree/c3358c2bb7e263095e9dea590f2b7f00add43ef0/public), [mensagens web](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/public/messaging.js) e [catálogos PT/EN](https://github.com/Renato1909/LensClick/tree/c3358c2bb7e263095e9dea590f2b7f00add43ef0/public/locales) |
| W8 | [Testes da plataforma](https://github.com/Renato1909/LensClick/tree/c3358c2bb7e263095e9dea590f2b7f00add43ef0/test) e [plano multicliente](https://github.com/Renato1909/LensClick/blob/c3358c2bb7e263095e9dea590f2b7f00add43ef0/docs/PLANO_ANDROID_VALIDACAO_PUBLICACAO.md) |
| A1 | [Telas e navegação Android](https://github.com/Renato1909/LensClickApp/blob/bd3c1408886b0279a1f8867805bd93785939bf21/app/src/main/java/com/example/lensclickapp/ui/LensClickApp.kt) e [ViewModel](https://github.com/Renato1909/LensClickApp/blob/bd3c1408886b0279a1f8867805bd93785939bf21/app/src/main/java/com/example/lensclickapp/ui/LensClickViewModel.kt) |
| A2 | [Entidades e dados de exemplo](https://github.com/Renato1909/LensClickApp/blob/bd3c1408886b0279a1f8867805bd93785939bf21/app/src/main/java/com/example/lensclickapp/data/LensClickData.kt) e [repositório local](https://github.com/Renato1909/LensClickApp/blob/bd3c1408886b0279a1f8867805bd93785939bf21/app/src/main/java/com/example/lensclickapp/data/LensClickRepository.kt) |

## Critérios de decisão

- **Obrigatória no primeiro lançamento (O):** proposta de gate para o primeiro cliente conectado, sem indicar que já esteja pronta.
- **Equivalente nativa (N):** preservar a capacidade com interação Android apropriada; não significa equivalência implementada ou testada.
- **Exclusiva da web com justificativa (W):** manter fora do aplicativo nesta proposta, com justificativa explícita.
- **Adiada com risco documentado (A):** depende de escopo/contrato; não manter simulação apresentada como operação real na release.
- **P0:** bloqueia integração/lançamento seguro. **P1:** próxima fatia funcional. **P2:** após aprovação específica.

Toda capacidade remota depende do contrato móvel aprovado (Gate C), ambiente controlado e testes de autorização. Isso vale também quando a API web correspondente já existe.

## Matriz funcional

| Capacidade | Web e API atuais | Android atual | Classe / prioridade | Dependências e risco | Testes e estado |
|---|---|---|---|---|---|
| Cadastro de cliente e fotógrafo | Cadastro real; `POST /api/auth/register`; papel profissional configurado pela conta. W2–W4 | Dois formulários; usuários e hash SHA-256 locais. A1/A2 | **O / P0** | Contrato móvel, telefone exigido pela API, política de papéis e consentimento; nunca migrar hash/senha demo ao servidor. | Backend: `auth-flow`, `auth-route-contracts`, `professional-profile`. Android remoto ausente; testar duplicidade, erro e papel. |
| Login, sessão, logout e revogação | Cookie de sessão e `GET /api/auth/me`; logout remove sessão. W2 | Validação Room; `currentUser` em memória; logout local. A1/A2 | **O / P0** | Não reutilizar cookie nem fabricar bearer/refresh; revogação, expiração, limite de sessões e Keystore pendentes. | Backend: `auth-flow`, `auth-route-contracts`, `account-management`. Exigir testes móveis de expiração/revogação e isolamento entre contas. |
| Recuperação/troca de senha | Solicitação e confirmação com token; Brevo; troca autenticada. W2/W3 | Recuperação apenas abre diálogo; menu de segurança sem implementação. A1 | **O / P0** | Fluxo controlado, mensagens sem enumeração e deep links aprovados; não prometer envio inexistente. | Backend: `password-reset`, `account-management`. Android pendente; uso único, expiração, indisponibilidade e revogação. |
| Verificação/reenvio de e-mail | `/api/auth/email/verify`, `/resend`; `emailVerified`; login em modo de aviso. W2 | Ausente. | **O / P0** | Preservar política compartilhada; decidir abertura dos links e revalidação após mudança de e-mail. | Backend: `email-verification`. Android pendente; token usado/expirado, reenvio limitado e aviso. |
| Dados da conta, foto e troca de papel | `PUT /api/profile`, `DELETE /api/account/photo`, imagem privada; `perfil.html`. W3/W7 | Conta exibe nome/e-mail; menus abrem aviso. Perfil profissional somente leitura após cadastro. A1 | **O / P1** | Mapear dados reais e reautenticação; foto/role podem despublicar perfil. Alternar modo visual não altera autorização. | Backend: `account-management`, `account-route-contracts`. Android pendente; acesso indevido, conflito, foto e despublicação. |
| Exportação e exclusão da conta | `GET /api/account/export`, `DELETE /api/account`; confirmação + senha; JSON sem credenciais/binários. W3 | Ausentes. | **O / P0** | Contrato de reautenticação, destino protegido do arquivo e limpeza de sessão/cache após exclusão. Não inferir conformidade jurídica. | Backend: `account-export`, `account-management`. Android pendente; acesso cruzado, cancelamento e limpeza local. |
| Descoberta, filtros e paginação | Busca publicada; cidade, UF, especialidade, disponibilidade, preços, raio e ordenação. W4/W7 | Lista Room, busca por nome/especialidade e categorias locais. A1/A2 | **O / P1** | DTOs e paginação; API não oferece busca por nome `q`. Preço de filtro em reais, resposta em centavos. | Backend: `search`, `search-interface`, `portfolio-public-route-contracts`. Android pendente; vazio, filtros, ordem e paginação. |
| Perfil público e leitura do portfólio | Perfil por `slug`, consentimento de publicação, contatos opcionais e imagens reais. W4/W5 | Tela fixa de exemplo; seleção não transporta identidade do fotógrafo; fotos/estatísticas fixas. A1 | **O / P1** | Navegação por `slug` validado; ocultar contatos não consentidos; 404 para perfil removido/despublicado. | Backend: `professional-profile`, `portfolio-public-route-contracts`. Android pendente; identidade selecionada, privacidade e imagens. |
| Perfil profissional e publicação | Rascunho, validação de publicação, especialidades, raio, preço em centavos e disponibilidade. W4 | Campos simplificados no cadastro local; sem publicação/rascunho remoto. A1/A2 | **O / P1** | Respeitar validação e enumerações do backend; mantenedor aprova fotógrafo no primeiro lançamento. | Backend: `professional-profile`. Android pendente; rascunho, consentimento, validação e despublicação. |
| Edição/envio de portfólio | Até 12 imagens; upload, legenda, alt text, categoria, capa, ordem e exclusão. W5 | Galeria ilustrativa; sem entidade/upload de portfólio. A1/A2 | **N / P1** | Se fotógrafo entrar no lançamento: seletor Android, compressão e upload conforme limites; não pedir permissão ampla sem necessidade. | Backend: `portfolio-public-route-contracts`, `portfolio-cover-category`. Android pendente; limites, interrupção, autoria e acessibilidade. |
| Avaliações verificadas | Solicitação de contato, confirmação pelo fotógrafo, nota/comentário e moderação. W4 | Nota e quantidade fixas, sem escrita nem elegibilidade. A1 | **O / P1** | Reusar fluxo existente e reputação autoritativa; remover métricas fictícias da release. | Backend: `reviews`, `reputation-interface`. Android pendente; elegibilidade, duplicata, edição/exclusão e moderação. |
| Conversas e mensagens | Inbox persistente, paginação, não lidas, envio e leitura; HTTP. W3/W4/W7 | Lista derivada dos fotógrafos; conversa fixa; mensagens enviadas só em estado Compose. A1 | **O / P1** | Participantes/ID reais; sem promessa de tempo real, presença online ou anexos; tratamento de conversa encerrada. | Backend: `messaging`, `messaging-interface`. Android pendente; isolamento, paginação, 409/429 e envio repetido. |
| Indicador de não lidas | `unreadCount` no inbox e `PUT .../read`; UI web. W3/W7 | Indicador visual fixo. A1 | **N / P1** | Usar contagem remota e ciclo de atualização aprovado; não inventar push. | Backend: `messaging`. Android pendente; leitura em dois clientes e retorno do background. |
| Push/notificações do sistema e presença | Nenhuma rota de registro de dispositivo, push, WebSocket/SSE ou presença no catálogo/handlers auditados. W1/W6 | Menu de notificações e indicador de online demonstrativos. A1 | **A / P2** | Risco: mensagens não percebidas fora do app. Exige decisão de produto, privacidade e backend; sem credenciais/SDK nesta fase. | Sem contrato ou teste end-to-end correspondente; tornar limitação explícita antes do lançamento. |
| Solicitação de orçamento e propostas | **Sem rota ou tabela** de orçamento/proposta; `review-requests` serve à avaliação. W1/W4/W6 | `Budget` local; formulário e abas/status ilustrativos; sem dono ou fotógrafo associado. A1/A2 | **A / P0** | Lacuna de produto: decidir remover/ocultar na release ou priorizar contrato na plataforma. Risco de prometer contratação inexistente. Nunca mapear para avaliação. | Sem contrato/teste de integração; definir estados, autorização e transições no backend antes do Android. |
| Painel profissional, pedidos e agenda | Sem dashboard de agendamentos/contratações; área web é conta/perfil. W1/W6/W7 | Totais/filtros derivados dos orçamentos locais e textos de demonstração. A1 | **A / P0** | Mesmo gate de orçamentos; risco de exibir eventos/receita/confirmações sem lastro. | Sem contrato. Não classificar simples navegação local como gestão de agenda concluída. |
| Favoritos | Sem rota/tabela de favoritos no catálogo/esquema. W1/W6 | Estado Compose e aba demonstrativa; sem persistência/sincronização de favoritos. A1 | **A / P2** | Risco de perda e inconsistência entre clientes; decidir local por dispositivo ou conta antes de implementar. | Sem contrato. Testar persistência/limpeza somente após decisão de escopo. |
| Onboarding, voltar e deep links | URLs públicas por `slug` e links web de recuperação/verificação/conversa. W2/W7 | `Screen` + estado salvo; sem back stack/deep link Android. A1 | **N / P0** | Navigation Compose; domínio e associação Android App Links dependem do mantenedor; controlar destinos privados/expirados. | Testes Android pendentes de voltar, rotação, recriação, link malformado e sessão encerrada. |
| Internacionalização | Catálogos PT-BR/EN-US e runtime web. W7 | Textos majoritariamente embutidos em Kotlin, PT-BR. A1 | **N / P1** | Extrair recursos Android; equivalência PT/EN e formatos de moeda/data, sem traduzir valores de enums do contrato. | Backend/UI: `i18n-catalogs`, `translation-transition`. Android pendente; recursos completos, locale e expansão de texto. |
| Acessibilidade | Alt text de portfólio obrigatório; controles/feedback com atributos acessíveis. W5/W7 | Compose com rótulos variáveis, ícones em texto e fontes pequenas; sem validação TalkBack. A1 | **N / P0** | Equivalência exige foco, semântica, contraste, fonte ampliada e toque; atributos web não provam acessibilidade Android. | Web: `responsive-portfolio-interface`, `messaging-interface`; Android pendente de dispositivo e inspeção manual. |
| Erro, carregamento, rede instável e cache | Respostas HTTP/JSON, rate limit e cache público da edge. W1–W5 | Dados locais não exercitam rede; estados parciais de envio/login. A1/A2 | **N / P0** | Erros tipados, timeout e repetição controlada; Room futuro como cache por usuário, sem autoridade sobre permissões/regras. | Backend: suites de contratos e `edge-cache`. Android pendente; 401/403/404/409/429/5xx, timeout e isolamento de cache. |
| Escritas offline e sincronização em segundo plano | Sem protocolo de fila, idempotência ou reconciliação móvel identificado. W1 | Gravações locais não sincronizáveis. A2 | **A / P2** | Risco de duplicação, divergência e perda. Não transformar demo local em fila remota; exigir contrato próprio na plataforma. | Sem contrato; não liberar retry automático de mutações até definir semântica. |
| Termos, privacidade, consentimento e suporte | `termos.html`, `privacidade.html`, contato persistido; revisão legal/domínio ainda gates documentados. W6–W8 | Checkbox de Termos sem documento navegável; ajuda/sobre abrem aviso. A1 | **O / P0** | Textos e URLs aprovados, identidade do controlador, canal de suporte e mapa de dados reais; sem inventar decisões jurídicas. | Android pendente: abertura acessível, consentimento e consistência com a coleta real; revisão jurídica externa. |
| Contato institucional e newsletter | `POST /api/contact`, `POST /api/newsletter`; landing. W6/W7 | Sem equivalente completo. | **W / P2** | Formulários institucionais permanecem na web para evitar duplicar aquisição de marketing; app ainda precisa suporte acessível (linha anterior). | Backend: `public-input-route-contracts`. Aceite de escopo pendente; encaminhamento web deve usar URL aprovada. |
| SEO, SSR, sitemap e robots | `/fotografo/:slug`, metadados, sitemap e robots dinâmicos. W2/W7 | Não aplicável à renderização nativa. | **W / P2** | Indexação permanece responsabilidade web; compartilhamento/deep link Android é equivalente separado. | Backend: `indexable-profile`, `sitemap-robots`, `seo-static-metadata`; manter cobertura web. |
| Administração e moderação | Cinco operações `/api/admin/*`, `requireAdmin`, painel `admin.html`. W6/W7 | Ausente. | **W / P1** | Concentrar ferramentas sensíveis na web no primeiro lançamento; servidor continua aplicando moderação a todos os clientes. Mantenedor pode rever escopo. | Backend: `admin-authorization`, `admin-moderation`. App precisa refletir 403 e conteúdo moderado; sem autoatribuir admin. |
| 2FA compartilhado | Sem rotas/implementação; planejado na plataforma. W1/W8 | Ausente. | **A / P0** | Threat model e decisão multicliente pendentes. Risco de tomada de conta só com senha; não inventar política exclusiva Android. Avaliar se será gate do lançamento. | Contrato e testes ainda inexistentes; aceite explícito de risco ou implementação backend antes da release. |

## Contratos existentes relevantes à integração futura

São contratos **web atuais**, não endpoints móveis aprovados. Caminhos relativos abaixo não autorizam escolher uma URL de produção para o app.

### Autenticação e conta

| Operações existentes | Dados/comportamento observado | Diferença que bloqueia integração direta |
|---|---|---|
| `POST /api/auth/register`; `POST /api/auth/login`; `GET /api/auth/me`; `POST /api/auth/logout` | Cadastro recebe `name`, `email`, `phone`, `password`, `language` opcional; login recebe `email/password`. Resposta `{ user }`; sessão entregue por `Set-Cookie`, não JSON bearer. Cadastro cria cliente; perfil/papel é etapa posterior. | Android não coleta telefone no cadastro; os papéis locais e IDs Room não são identidade remota. |
| `POST /api/auth/password-reset/request`; `POST /api/auth/password-reset/confirm` | Solicitação `{ email, language }`, resposta genérica `202`; confirmação `{ token, password }`; token de uso único com TTL 30 minutos. Recuperação invalida todas as sessões. | Transporte móvel do link/token, expiração e retomada precisam de contrato. Nenhuma senha deve entrar no Room. |
| `POST /api/auth/email/verify`; `POST /api/auth/email/resend` | Verificação `{ token }`, token de uso único com TTL 24 horas; reenvio autenticado, `language` opcional. `user.emailVerified` informa o estado; login não bloqueia e-mail pendente. | Deep links e política após alteração de e-mail precisam de validação multicliente. |
| `PUT /api/profile`; `PUT /api/account/password` | Perfil recebe nome/e-mail/telefone/papel/idade/gênero/foto; mudança de e-mail exige `currentPassword`. Senha recebe `currentPassword/newPassword`; encerra outras sessões e mantém a atual. | Não adotar troca de papel silenciosa para simular modo cliente. Foto é data URL de JPG/PNG/WebP com até 450.000 bytes; preservar referência atual quando aplicável. |
| `GET /api/profile-picture/:id`; `DELETE /api/account/photo` | Foto privada por dono; remoção despublica perfil profissional. | Cache e logs não podem expor imagem privada entre contas. |
| `GET /api/account/export`; `DELETE /api/account` | Exportação JSON inclui dados relacionados, sem credenciais/binários; exclusão exige `currentPassword` e `confirmation` igual a `EXCLUIR` ou `DELETE`. | Destino do arquivo, reautenticação e descarte do cache local precisam de UX segura. |

`publicUser` contém `id`, `name`, `email`, `phone`, `role`, `age`, `gender`, `emailVerified`, `isAdmin`, `profilePic`; URLs de imagem são relativas. A sessão usa cookie `lensclick_session`, `HttpOnly`, `SameSite=Lax`, `Secure` em HTTPS, TTL de sete dias e até cinco sessões por usuário. As escritas exigem `Origin` igual à origem do pedido. Não existe bearer, renovação móvel ou registro de dispositivos no catálogo auditado. **Não forjar Origin nem copiar cookies para contornar esse gate.** W1–W3.

### Descoberta, perfil e portfólio

- `GET /api/photographers`: parâmetros `city`, `state`, `specialty`, `availability`, `minPrice`, `maxPrice`, `minRadius`, `sort`, `page`, `pageSize`; padrão de página 12, máximo 50. Ordenação: `recommended`, `recent`, `price_asc`, `price_desc`, `radius_desc`, `rating_desc`. Responde `{ photographers, pagination, sort }`. `minPrice/maxPrice` usam reais; `startingPriceCents` no DTO usa centavos. **Não há filtro por nome/termo livre** no handler atual.
- `GET /api/photographers/:slug`, `GET /api/photographers/:slug/photo`, `GET /api/photographers/:slug/portfolio/:id`: perfil, foto e imagens públicas condicionados à publicação. Detalhe retorna `{ profile }` com portfólio. Slug deve seguir o formato validado pelo roteador; não é ID sequencial local.
- `GET` e `PUT /api/account/professional-profile`: rascunho/publicação do fotógrafo. Campos incluem `bio`, `specialties`, `city`, `state`, `serviceRadiusKm`, `startingPriceCents`, `availability`, contatos, `showEmail`, `showPhone`, `isPublished`. Publicação exige foto, bio suficiente, especialidade, localização e contato público; autorização e critérios pertencem ao servidor.
- `GET`, `POST` e `PUT /api/account/portfolio`; `GET /api/account/portfolio/:id/image`; `DELETE /api/account/portfolio/:id`: upload usa `multipart/form-data` com `image`, `altText`, `caption`, `category`; JPG/PNG/WebP até **4.000.000 bytes**, máximo **12 itens**. Atualização recebe `items` com metadados/ordem e no máximo uma capa. DTO contém `id`, `caption`, `altText`, `position`, `isCover`, `category`, `imageUrl`.

Fontes: W1/W2/W4/W5. Valores monetários, enumerações e IDs precisam de mapeadores próprios; não serializar diretamente entidades Room de demonstração.

### Avaliações e mensagens

- `GET /api/photographers/:slug/reviews` lê reputação. `POST /api/photographers/:slug/review-requests` recebe `{ message }` e cria solicitação de **contato para avaliação**, apenas para `client`. `GET /api/account/review-requests` lista as solicitações do papel atual; `PUT /api/account/review-requests/:id` recebe `status: confirmed|rejected`, autorizado ao fotógrafo. `POST /api/account/review-requests/:id/review` publica `{ rating, comment }` somente após confirmação. `PUT`/`DELETE /api/account/reviews/:id` edita/remove avaliação autorizada. Nota inteira de 1 a 5; comentário de pelo menos 10 caracteres. Nada disso representa proposta comercial ou reserva.
- `POST /api/photographers/:slug/conversations` inicia/reutiliza conversa com `{ message }`, autorizado ao cliente. `GET /api/account/conversations` retorna `{ conversations, pagination }`, com `unreadCount` e contraparte; página padrão 20, máximo 50. `GET /api/account/conversations/:id/messages` usa cursor `before` e `pageSize` padrão 50, máximo 100; retorna `{ messages, pagination: { hasMore, nextBefore } }`. `POST` nesse caminho envia `{ message }` com até 2.000 caracteres. `PUT /api/account/conversations/:id/read` marca mensagens recebidas como lidas.
- Mensagens exigem participação na conversa; conversa encerrada retorna `409` ao tentar enviar. UI web carrega/atualiza por chamadas HTTP acionadas pelo fluxo; não foi encontrado transporte de tempo real/push/presença no catálogo ou no script auditado.

Fontes: W1/W3/W4/W7. O Android deve decidir com o backend como um fotógrafo também poderá contratar; mudar apenas o menu não satisfaz `role === 'client'`.

### Operações que permanecem na plataforma

`GET /api/admin/overview`, `GET /api/admin/reviews`, `PUT /api/admin/reviews/:id`, `GET /api/admin/conversations`, `PUT /api/admin/conversations/:id` exigem admin. `POST /api/contact` e `POST /api/newsletter` atendem entradas institucionais. `GET /api/health` verifica Worker/D1, sem equivaler a teste de todas as integrações. W1/W6.

## Riscos e decisões antes da próxima fatia

1. **Contrato móvel ausente:** aprovar no repositório da plataforma login, expiração, renovação/revogação, reautenticação, limite de sessões, política de 2FA e armazenamento protegido; definir ambiente de teste. Não existe endpoint móvel a ser “apenas conectado”.
2. **Escopo comercial divergente:** Android mostra orçamento/proposta/agenda que o backend não oferece. Mantenedor decide exclusão do primeiro lançamento ou implementação centralizada na plataforma. Não criar regra ou banco paralelo no app.
3. **Papéis divergentes:** Android alterna modo mantendo `photographer`, enquanto criação de conversa/solicitação de avaliação exige `client`; escolher política compartilhada antes de reproduzir a UX.
4. **Privacidade e dados demonstrativos:** esconder/remover dados, credenciais e estatísticas fictícias da release; nenhum usuário real deve ser autenticado por Room. Cache remoto deve ser particionado e limpo conforme sessão/conta.
5. **Compatibilidade ainda implícita:** as rotas não têm prefixo de versão móvel e erros usam `{ error: texto }`; pactuar códigos estáveis, versionamento, campos opcionais, limites, paginação e repetição de mutações. O `X-Request-ID` pode ajudar suporte sem registrar tokens ou dados pessoais.
6. **Estado de e-mail exige revisão backend:** no handler auditado, a troca de e-mail remove outras sessões/tokens de recuperação, mas não redefine explicitamente `email_verified_at`. Levar esse caso ao contrato e aos testes da plataforma antes de interpretar `emailVerified` no Android; nenhuma correção de produção foi feita nesta fase.
7. **Decisões institucionais pendentes:** pacote definitivo, domínios/links, identidade/controlador, revisão de termos/privacidade, idiomas e escopo do fotógrafo/2FA/admin. A matriz não substitui esses aceites.

## Critério para atualizar a matriz

Em cada fatia, registrar novo SHA da plataforma, operações aprovadas, testes de contrato em ambiente controlado, evidência Android (incluindo falhas e dispositivo quando aplicável), estado das linhas e aceite do mantenedor. “API existente” ou “tela navegável” não devem ser promovidos a “paridade concluída” sem validar o fluxo e suas permissões nos dois clientes.
