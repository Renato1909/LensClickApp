<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.LensClickApp" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen (API 12+ e, via core-splashscreen, em versões anteriores).
         O ícone usa uma caixa de 288dp com o conjunto da marca dentro do círculo de
         192dp que o sistema exibe — daí o arquivo @drawable/splash_icon já vir com
         a folga correta, em vez de reutilizar o PNG "cru" da marca. -->
    <style name="Theme.LensClickApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.LensClickApp</item>
    </style>
</resources>
